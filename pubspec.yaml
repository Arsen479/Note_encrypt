name: flutter_zametki_with_encrypt_univer
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
    
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  encrypt: ^5.0.3  
  local_auth: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/keys/public_key.pem
    - assets/keys/rsa_private_key.pem
